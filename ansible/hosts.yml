all:
  children:
    homelab:
      hosts:
        raspi:
          ansible_host: 192.168.100.96
          ansible_user: vova
          ansible_python_interpreter: /usr/bin/python3
          ansible_ssh_private_key_file: ~/.ssh/laptop_key
      vars:
#        external_disks:
#          - name: cryobs_main
#            partition_id: "ata-ST500LT012-9WS142_W0VF1QAL-part1"
#            mount_point: /mnt/cryobs
#            directory_structure:
#              services: {}
#              data: {}
#              backups: {}

        # Network
        homelab_network_name: homelab
        homelab_subnet: 172.20.0.0/16
        
        # Domain
        homelab_domain: "{{ vault_cloudflare_domain }}"
        
        # Cloudflare Tunnel
        cloudflare_tunnel_enabled: true
